\documentclass[letterpaper,
            11pt]
            {moderncv}
\setcounter{tocdepth}{3}
\usepackage{geometry}
\geometry{letterpaper,left=25mm,right=20mm, bottom=30mm}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{wrapfig}
\usepackage{times} 
\usepackage{lastpage}
% Biblatex with BIBER---------------------------------
\usepackage[style=apa,sortcites=true,sorting=ydnt,backend=biber]{biblatex}
\DeclareLanguageMapping{english}{english-apa}
$for(bibliography)$\addbibresource{$bibliography$}
$endfor$
\moderncvtheme[$cvoption$]{$moderncvtheme$}

\usepackage{fancyhdr}
\pagestyle{fancy}

%-----------------------------------------------------
% hebe mich als Autoren hervor
% erfordert manuelle Bearbeitung der .bib-Datei,
% ergänze bei jedem Eintrag
%     author+an = {3=highlight},
% und ersetze die "3" durch deine Position
% in der Autorenreihe
\renewcommand*{\mkbibnamegiven}[1]{%
  \ifitemannotation{highlight}
    {\textbf{#1}}
    {#1}}

\renewcommand*{\mkbibnamefamily}[1]{%
  \ifitemannotation{highlight}
    {\textbf{#1}}
    {#1}}
%-----------------------------------------------------

\rfoot{\textit{\small{\thepage/\pageref{LastPage}}}}
\name{$vorname$}{$nachname$}
\firstname{$vorname$}
\familyname{$nachname$}
$if(ort)$\address{$strasse$}{$ort$}$endif$
$if(telefon)$\mobile{$telefon$}$endif$
\email{$email$}
$if(extrainfo)$
\extrainfo{$extrainfo$}
$endif$
$if(passbild)$\photo[64pt][0.4pt]{$passbild$}$endif$
\title{$graduate$}



% Uniform name color — both parts in dark gray
\renewcommand*{\firstnamestyle}[1]{{\firstnamefont\color{color2!85!black}#1}}

% Pandoc compact-list compatibility
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Author name highlighting — \myname{} in .bib files bolds the author
\newcommand{\myname}[1]{\textbf{#1}}
\renewcommand*{\mkbibnamegiven}[1]{%
  \ifitemannotation{highlight}{\textbf{#1}}{#1}}
\renewcommand*{\mkbibnamefamily}[1]{%
  \ifitemannotation{highlight}{\textbf{#1}}{#1}}

\begin{document}

\makecvtitle


$body$

$if(publikationen)$
%----------CAVE ! --------------------------------------------
% this "\nocite{*}" command has to be set. 
% it will mark all non-cited entries for bibliography.
% Otherwise, you will get "empty bibliography"
\nocite{*}
%-------------------------------------------------------------
$for(publikationen)$
$for(it.list)$
\printbibliography[keyword={$it.key$}, title={$it.title$}$if(subbibliography)$, heading=subbibliography$endif$]
$endfor$
$endfor$
$endif$

\end{document}